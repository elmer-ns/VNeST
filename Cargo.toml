[package]
name = "VNeST"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
image = { version = "0.25.6", features = ["ico"] }
slint = "1.8.0"
text-measurer-harfbuzz = {path = "../../text-measurer-harfbuzz"}

[build-dependencies]
slint-build = "1.8.0"
